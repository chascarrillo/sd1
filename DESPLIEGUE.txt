1 - compilar la interfaz (InterfRemSondas.java)

	si no funciona el comando, encontrar la ruta de los ejecutables de java con
		which javac
	y añadirla a PATH con
		export PATH=$PATH:<resultado which javac>

2 - compilar el objeto remoto (ObjetoRemSondas.java)

	si no funciona, compilar interfaz y objeto remoto a la vez con
		javac InterfRemSondas.java ObjetoRemSondas.java

	si sigue sin funcionar, borra la primera linea (package sondasRMI;) de los archivos y prueba de nuevo
	Debemos incluir en la variable de entorno CLASSPATH la ruta a la interfaz remota para compilar el objeto remoto.
		export CLASSPATH=$CLASSPATH:<ruta>

3 - generar el stub de ObjetoRemSondas (mediante)
		rmic ObjetoRemSondas

4 - Controller usa la interfaz y el stub para comunicarse asi que lo metemos en un jar para transportarlo
		jar cvf interstub.jar InterfRemSondas.class ObjetoRemSonda_Stub.class

5 - compilamos el registro
		javac RegistroSondas.java

6 - 